# ☀️ Morning Brief 사용설명서

매일 아침 7시에 자동으로 뉴스+시장 데이터를 모아 Claude가 브리핑을 만들어 이메일로 보내주는 시스템.

---

## 📁 폴더 구조
```
~/morning_brief/
├── config.py         ⚙️ 설정 (뉴스 주제·티커·모델·브리핑 형식) ← 대부분 여기만 수정
├── main.py           전체 실행 파일
├── .env              🔑 API 키 (비밀! 공유 금지)
├── modules/          기능별 코드
│   ├── news.py       뉴스 수집
│   ├── market.py     시장 데이터
│   ├── brief.py      Claude 브리핑
│   └── email_send.py 이메일 발송
├── logs/             날짜별 실행 로그
└── test_*.py         기능별 개별 테스트
```

---

## ▶️ 지금 당장 한 번 실행 (7시 안 기다리고)
```bash
cd ~/morning_brief && ./venv/bin/python main.py
```

## 🧪 기능별 개별 테스트
```bash
cd ~/morning_brief
./venv/bin/python test_news.py      # 뉴스만
./venv/bin/python test_market.py    # 시장만
./venv/bin/python test_brief.py     # 브리핑 생성만 (이메일 X)
./venv/bin/python test_email.py     # 이메일만
```

---

## ✏️ 커스터마이징 (전부 `config.py` 수정)

### 뉴스 주제/키워드 추가
`NEWS_TOPICS` 에서 원하는 카테고리의 `queries` 리스트에 `"키워드"` 추가.
새 카테고리는 `{}` 블록 통째로 복사해서 붙여넣고 내용만 변경.

### 새 종목/지수 추가
`MARKET_TICKERS` 에 한 줄 추가. 심볼은 finance.yahoo.com 에서 검색.
예) 삼성전자 `"005930.KS"`, 애플 `"AAPL"`, 비트코인 `"BTC-USD"`, 테슬라 `"TSLA"`

### 브리핑 형식·말투 바꾸기
`BRIEF_INSTRUCTIONS` 문자열 수정 (섹션 추가/삭제, 톤 변경 자유롭게).

### 모델 바꾸기 (비용 조절)
`CLAUDE_MODEL`:
- `"claude-opus-4-8"` 최고 품질 (기본)
- `"claude-sonnet-5"` 가성비 (품질 좋고 저렴) ← 비용 아끼려면 이걸로
- `"claude-haiku-4-5-20251001"` 초저가

---

## ⏰ 실행 시간 바꾸기 (예: 7시 → 6시 30분)
1. 설정 파일 열기:
   ```bash
   open -e ~/Library/LaunchAgents/com.yechan.morningbrief.plist
   ```
2. `<key>Hour</key><integer>7</integer>` 의 숫자 7 → 원하는 시(0~23)로,
   `<key>Minute</key><integer>0</integer>` 의 0 → 원하는 분으로 수정 후 저장.
3. 다시 로드:
   ```bash
   launchctl bootout gui/$(id -u)/com.yechan.morningbrief
   launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.yechan.morningbrief.plist
   ```

## ⏸️ 자동 실행 잠시 끄기 / 완전 중단
```bash
# 끄기 (등록 해제)
launchctl bootout gui/$(id -u)/com.yechan.morningbrief

# 다시 켜기
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.yechan.morningbrief.plist
```

## ✅ 자동 실행 등록됐는지 확인
```bash
launchctl list | grep morningbrief
```
→ `com.yechan.morningbrief` 가 나오면 등록된 상태.

## 🔧 강제로 지금 자동실행 테스트
```bash
launchctl kickstart -k gui/$(id -u)/com.yechan.morningbrief
```

---

## 🐛 문제 생겼을 때 (디버깅)
1. **오늘 실행 로그 보기**:
   ```bash
   cat ~/morning_brief/logs/$(date +%Y-%m-%d).log
   ```
2. **자동실행(launchd) 자체 에러 로그**:
   ```bash
   cat ~/morning_brief/logs/launchd_err.log
   ```
3. 로그에 `실패` / `ERROR` 있으면 그 부분이 원인. 각 기능은 독립적이라
   하나 실패해도 나머지는 정상 진행됨 (이메일에도 "일부 실패" 표시됨).

## 📊 Excel 분석 워크북 (CFA)
매일 아침 `MorningBrief_Analysis.xlsx` 가 갱신되고 이메일에 첨부돼서 옴.

**시트 구성:**
- **Analysis** — CFA 대시보드. 최신가·기간별수익률·연환산수익/변동성·**샤프비율**·이동평균(20/60/200)·52주고점대비. 전부 Excel 공식이라 데이터 바뀌면 자동 재계산.
  - 🟡 노란 `rf` 셀(B2, 기본 3%)을 바꾸면 샤프비율이 즉시 다시 계산됨.
- **Prices** — 일별 종가 (2년치)
- **Returns** — 일별 수익률
- **Correlation** — 자산 간 상관계수
- **검증(Python)** — 같은 지표를 파이썬으로 계산한 값 (공식 결과와 대조용)

**Excel만 따로 다시 만들기:**
```bash
cd ~/morning_brief && ./venv/bin/python test_excel.py
```

**종목 추가/변경:** `config.py` 의 `MARKET_TICKERS` 만 고치면 이메일·Excel 둘 다 자동 반영 (Part 1 가이드 참고).

**분석 원칙:** 주말·진행중인 오늘은 빼고 **"직전 완료 거래일"까지** 분석 (아침마다 진짜 등락률이 나오게). 그래서 저녁에 돌리면 오늘 한국장 종가는 아직 안 들어감 — 정상이야.

**직접 만든 시트는 보존됨:** 네가 워크북에 'MyNotes' 같은 시트를 새로 만들어 메모하면, 자동 갱신 때 안 지워져 (Analysis/Prices/Returns/Correlation/검증 시트만 매번 새로 씀).

## ⚠️ 알아둘 점
- **Mac이 꺼져 있거나 잠들어 있으면** 7시에 못 돌아. 깨어난 뒤(로그인 상태) 실행돼.
  매일 확실히 받으려면 잘 때 전원 연결 + 뚜껑 닫아도 되게 설정하거나, 아침에 Mac을 켜두면 됨.
- **비용**: 하루 브리핑 1건 ≈ 몇십 원. opus 기준. sonnet-5로 바꾸면 더 저렴.
- **.env 파일은 절대 공유 금지** (API 키 = 비밀번호).
